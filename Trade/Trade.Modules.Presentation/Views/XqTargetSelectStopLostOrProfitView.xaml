<UserControl x:Class="XueQiaoWaf.Trade.Modules.Presentation.Views.XqTargetSelectStopLostOrProfitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <StackPanel Margin="10">
        <Button Content="止损买入" Command="{Binding ToStopLostBuyCmd}" Style="{StaticResource FlatContentButtonPrimary3}"/>
        <Button Content="止损卖出" Command="{Binding ToStopLostSellCmd}" Style="{StaticResource FlatContentButtonPrimary3}" Margin="0,8,0,0"/>
        <Button Content="止盈买入" Command="{Binding ToStopProfitBuyCmd}" Style="{StaticResource FlatContentButtonPrimary3}" Margin="0,8,0,0"/>
        <Button Content="止盈卖出" Command="{Binding ToStopProfitSellCmd}" Style="{StaticResource FlatContentButtonPrimary3}" Margin="0,8,0,0"/>
    </StackPanel>
</UserControl>
